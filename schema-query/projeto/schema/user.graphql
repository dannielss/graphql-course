type Profile {
  id: ID!
  type: String!
}

enum UserStatus {
  ATIVO,
  INATIVO,
  BLOQUEADO
}

type User {
  id: ID!
  name: String!
  email: String!
  age: Int
  salary: Float
  vip: Boolean
  profile: Profile
  status: UserStatus
}